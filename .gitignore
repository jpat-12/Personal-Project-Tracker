# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# Secrets — never commit the real Telegram token / chat id
config.env

# Local database + SQLite sidecars (state lives on the server)
*.db
*.db-wal
*.db-shm
state.json
